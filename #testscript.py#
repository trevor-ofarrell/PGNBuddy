import json
import requests

re = requests.get("{}{}".format(
            "https://lichess.org/api/games/user/",
            username
            ), 
            params={
                "pgnInJson": "true",
                "opening": "true",
                "since": int(startdate) * 1000,
                "until": int(enddate) * 1000,
                "max": gamelen
            },
            headers={
                "Accept": "application/x-ndjson"
            },
            stream=True
        )

        data = re.text.replace("'", '"')
        print(data, sys.stderr)
        game_list = json.loads(data)
        print(game_list, file=sys.stderr)